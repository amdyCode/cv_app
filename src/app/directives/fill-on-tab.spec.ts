import { FillOnTab } from './fill-on-tab';

describe('FillOnTab', () => {
  it('should create an instance', () => {
    const directive = new FillOnTab();
    expect(directive).toBeTruthy();
  });
});
